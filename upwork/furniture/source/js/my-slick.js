jQuery(document).ready(function($) {
	$('.clients__list').slick({
		infinite: true,
		arrows: false,
		dots: true,
		slidesToShow: 2,
		slidesToScroll: 2,
		// centerMode: true,
		variableWidth: true		
	});	
});
